---
// dashboard.astro — Trang dashboard sau khi đăng nhập
const v = '20260824-21';
---
<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Dashboard — 2TF529</title>
  <meta name="robots" content="noindex,nofollow">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <script is:inline>
    (function(){
      var d = document.documentElement;
      d.setAttribute('data-theme', localStorage.getItem('theme') || (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'));
    })();
  </script>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    [hidden] { display: none !important; }

    :root {
      --sb-w: 240px;
      --bg: #0b0f1a;
      --surface: #111827;
      --surface2: #1a2235;
      --border: rgba(255,255,255,.07);
      --text: #f0ece4;
      --text2: #8b8fa8;
      --accent: #4f8ef7;
      --accent2: #f5c842;
      --green: #22c55e;
      --red: #ef4444;
      --amber: #f59e0b;
      --r: 14px;
      --shadow: 0 4px 24px rgba(0,0,0,.35);
    }

    html[data-theme="light"] {
      --bg: #f4f7fb;
      --surface: #ffffff;
      --surface2: #eef3f9;
      --border: #dbe3ef;
      --text: #162033;
      --text2: #667085;
      --accent: #2563eb;
      --accent2: #d79a12;
      --green: #059669;
      --red: #dc2626;
      --amber: #d97706;
      --shadow: 0 10px 30px rgba(30,64,175,.10);
      color-scheme: light;
    }

    body { font-family: 'Inter',sans-serif; background: var(--bg); color: var(--text); min-height: 100vh; display: flex; overflow-x: hidden; }

    /* ===== SIDEBAR ===== */
    .sidebar {
      width: var(--sb-w); flex-shrink: 0;
      background: var(--surface); border-right: 1px solid var(--border);
      display: flex; flex-direction: column;
      position: fixed; top: 0; left: 0; bottom: 0; z-index: 100;
      transition: transform .3s cubic-bezier(.4,0,.2,1);
    }
    .sb-logo { padding: 20px 20px 16px; border-bottom: 1px solid var(--border); display: flex; align-items: center; gap: 12px; text-decoration: none; }
    .sb-logo-mark { width: 36px; height: 36px; background: linear-gradient(135deg,#3a6ab8,#4f8ef7); border-radius: 10px; display: grid; place-items: center; font-weight: 900; font-size: 13px; color: #fff; letter-spacing: -.5px; box-shadow: 0 4px 12px rgba(79,142,247,.35); flex-shrink: 0; }
    .sb-logo b { display: block; font-size: 14px; font-weight: 800; color: var(--text); }
    .sb-logo small { font-size: 11px; color: var(--text2); }
    .sb-user { padding: 14px 16px; border-bottom: 1px solid var(--border); display: flex; align-items: center; gap: 10px; }
    .sb-avatar { width: 38px; height: 38px; border-radius: 50%; background: linear-gradient(135deg,#3a6ab8,#4f8ef7); display: grid; place-items: center; font-weight: 800; font-size: 14px; color: #fff; flex-shrink: 0; overflow: hidden; position: relative; }
    .sb-avatar img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
    .sb-uname { font-size: 13.5px; font-weight: 700; color: var(--text); }
    .sb-usub { font-size: 11.5px; color: var(--text2); margin-top: 1px; }
    .sb-nav { flex: 1; overflow-y: auto; padding: 12px 10px; display: flex; flex-direction: column; gap: 2px; }
    .sb-section { font-size: 10px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px; color: var(--text2); padding: 10px 10px 4px; }
    .sb-link { display: flex; align-items: center; gap: 10px; padding: 9px 12px; border-radius: 10px; text-decoration: none; color: var(--text2); font-size: 13.5px; font-weight: 600; transition: background .15s, color .15s; }
    .sb-link:hover { background: var(--surface2); color: var(--text); }
    .sb-link.active { background: rgba(79,142,247,.15); color: var(--accent); }
    .sb-link .ni { font-size: 16px; width: 20px; text-align: center; flex-shrink: 0; }
    .sb-bottom { padding: 12px 10px; border-top: 1px solid var(--border); }
    .sb-logout { display: flex; align-items: center; gap: 10px; padding: 9px 12px; border-radius: 10px; background: transparent; border: none; color: #f87171; font-size: 13.5px; font-weight: 600; cursor: pointer; width: 100%; transition: background .15s; font-family: 'Inter',sans-serif; }
    .sb-logout:hover { background: rgba(239,68,68,.1); }

    /* ===== MAIN ===== */
    .main { margin-left: var(--sb-w); flex: 1; min-width: 0; }
    .topbar { background: rgba(11,15,26,.9); backdrop-filter: blur(12px); border-bottom: 1px solid var(--border); padding: 0 28px; height: 60px; display: flex; align-items: center; justify-content: space-between; position: sticky; top: 0; z-index: 50; }
    html[data-theme="light"] .topbar { background:rgba(255,255,255,.9); }
    html[data-theme="light"] .sidebar { box-shadow:8px 0 30px rgba(30,64,175,.05); }
    .topbar-l { display: flex; align-items: center; gap: 12px; }
    .topbar-title { font-size: 16px; font-weight: 800; color: var(--text); }
    .topbar-sub { font-size: 12px; color: var(--text2); }
    .topbar-r { display: flex; align-items: center; gap: 10px; }
    .topbar-btn { width: 36px; height: 36px; border-radius: 10px; background: var(--surface2); border: 1px solid var(--border); color: var(--text2); font-size: 16px; cursor: pointer; display: grid; place-items: center; transition: background .15s, color .15s; }
    .topbar-btn:hover { background: var(--surface); color: var(--text); }
    .topbar-profile { padding: 7px 16px; background: rgba(79,142,247,.15); border: 1px solid rgba(79,142,247,.25); border-radius: 10px; color: var(--accent); font-size: 13px; font-weight: 700; text-decoration: none; transition: background .15s; }
    .topbar-profile:hover { background: rgba(79,142,247,.25); }
    .hbtn { display: none; width: 36px; height: 36px; background: var(--surface2); border: 1px solid var(--border); border-radius: 10px; color: var(--text2); font-size: 16px; cursor: pointer; }

    .content { padding: 28px; }

    /* Welcome hero */
    .hero {
      border-radius: 20px; padding: 32px 36px;
      background: linear-gradient(135deg,#0f2040 0%,#1a3a60 50%,#0f2040 100%);
      position: relative; overflow: hidden;
      margin-bottom: 24px;
      display: flex; align-items: center; justify-content: space-between; gap: 20px;
      animation: fadeUp .5s ease both;
    }
    .hero::before { content:''; position:absolute; top:-40%; right:-10%; width:420px; height:420px; background:radial-gradient(circle,rgba(79,142,247,.18) 0%,transparent 70%); pointer-events:none; }
    .hero::after { content:''; position:absolute; bottom:-30%; left:20%; width:300px; height:300px; background:radial-gradient(circle,rgba(245,200,66,.08) 0%,transparent 70%); pointer-events:none; }
    @keyframes fadeUp { from { opacity:0; transform:translateY(20px); } to { opacity:1; transform:translateY(0); } }
    .hero-text { position:relative; z-index:1; }
    .hero-greet { font-size:13px; color:rgba(255,255,255,.45); letter-spacing:.5px; text-transform:uppercase; margin-bottom:6px; }
    .hero-name { font-size:28px; font-weight:900; color:#fff; line-height:1.1; margin-bottom:8px; }
    .hero-goal { font-size:14px; color:rgba(255,255,255,.55); display:flex; align-items:center; gap:8px; flex-wrap:wrap; }
    .goal-chip { background:rgba(245,200,66,.15); border:1px solid rgba(245,200,66,.3); color:#f5c842; padding:3px 12px; border-radius:50px; font-size:12.5px; font-weight:700; }
    .hero-streak { position:relative; z-index:1; text-align:center; background:rgba(255,255,255,.06); border:1px solid rgba(255,255,255,.1); border-radius:16px; padding:20px 28px; backdrop-filter:blur(8px); flex-shrink:0; }
    .streak-fire { font-size:32px; margin-bottom:4px; }
    .streak-num { font-size:36px; font-weight:900; color:#f5c842; line-height:1; }
    .streak-lbl { font-size:12px; color:rgba(255,255,255,.5); margin-top:4px; }

    /* Stats */
    .stats { display:grid; grid-template-columns:repeat(4,1fr); gap:16px; margin-bottom:24px; animation:fadeUp .5s .05s ease both; }
    .stat { background:var(--surface); border:1px solid var(--border); border-radius:var(--r); padding:20px; display:flex; flex-direction:column; gap:8px; transition:transform .2s,box-shadow .2s,border-color .2s; cursor:default; }
    .stat:hover { transform:translateY(-3px); box-shadow:var(--shadow); border-color:rgba(79,142,247,.2); }
    .stat-hd { display:flex; align-items:center; justify-content:space-between; }
    .stat-lbl { font-size:12px; font-weight:700; color:var(--text2); text-transform:uppercase; letter-spacing:.5px; }
    .stat-ico { font-size:18px; }
    .stat-val { font-size:36px; font-weight:900; color:var(--text); line-height:1; }
    .stat-sub { font-size:12px; color:var(--text2); }
    .stat.blue .stat-val { color:var(--accent); }
    .stat.green .stat-val { color:var(--green); }
    .stat.amber .stat-val { color:var(--amber); }
    .random-study-stat { text-align:left; font-family:inherit; cursor:pointer; position:relative; overflow:hidden; color:#fff; border-color:transparent; background:linear-gradient(135deg,#2563eb 0%,#7c3aed 62%,#db2777 130%); box-shadow:0 10px 28px rgba(79,70,229,.24); }
    .random-study-stat::after { content:'🎲'; position:absolute; right:-5px; bottom:-17px; font-size:78px; opacity:.11; transform:rotate(-12deg); }
    .random-study-stat .stat-lbl,.random-study-stat .stat-sub { color:rgba(255,255,255,.76); }
    .random-study-stat .stat-val { color:#fff; font-size:18px; line-height:1.3; }
    .random-study-stat:hover { border-color:rgba(255,255,255,.38); box-shadow:0 14px 34px rgba(79,70,229,.34); }
    .random-study-stat:disabled { opacity:.7; cursor:wait; }

    /* Section title */
    .stitle { font-size:15px; font-weight:800; color:var(--text); margin-bottom:14px; display:flex; align-items:center; gap:8px; }
    .stitle::after { content:''; flex:1; height:1px; background:var(--border); }

    /* Quick cards */
    .qgrid { display:grid; grid-template-columns:repeat(3,1fr); gap:14px; margin-bottom:28px; animation:fadeUp .5s .1s ease both; }
    .qcard { background:var(--surface); border:1px solid var(--border); border-radius:var(--r); padding:20px; text-decoration:none; display:flex; flex-direction:column; gap:10px; transition:transform .2s,box-shadow .2s,border-color .2s; position:relative; overflow:hidden; }
    .qcard::before { content:''; position:absolute; top:0; left:0; right:0; height:3px; background:var(--qa,var(--accent)); border-radius:3px 3px 0 0; opacity:0; transition:opacity .2s; }
    .qcard:hover { transform:translateY(-4px); box-shadow:var(--shadow); border-color:rgba(79,142,247,.2); }
    .qcard:hover::before { opacity:1; }
    .qcard-ico { font-size:28px; }
    .qcard-title { font-size:14.5px; font-weight:800; color:var(--text); }
    .qcard-desc { font-size:12.5px; color:var(--text2); line-height:1.5; }
    .qcard-arrow { margin-top:auto; font-size:12px; color:var(--accent); font-weight:700; }

    /* Dashboard grid */
    .dgrid { display:grid; grid-template-columns:1fr 360px; gap:20px; animation:fadeUp .5s .15s ease both; }
    .panel { background:var(--surface); border:1px solid var(--border); border-radius:var(--r); overflow:hidden; }
    .panel-hd { padding:18px 20px 14px; border-bottom:1px solid var(--border); display:flex; align-items:center; justify-content:space-between; }
    .panel-hd-t { font-size:15px; font-weight:800; color:var(--text); }
    .panel-hd-a { font-size:12.5px; color:var(--accent); text-decoration:none; font-weight:700; }
    .panel-hd-a:hover { text-decoration:underline; }
    .panel-body { padding:8px; }

    /* History rows */
    .hrow { display:flex; align-items:center; justify-content:space-between; padding:12px 14px; border-radius:10px; text-decoration:none; gap:12px; transition:background .15s; }
    .hrow:hover { background:var(--surface2); }
    .hrow-t { font-size:13.5px; font-weight:700; color:var(--text); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:300px; }
    .hrow-d { font-size:11.5px; color:var(--text2); margin-top:2px; }
    .hscore { font-size:20px; font-weight:900; flex-shrink:0; min-width:48px; text-align:right; }
    .hscore.g { color:var(--green); }
    .hscore.o { color:var(--amber); }
    .hscore.b { color:var(--red); }
    .hscore.d { color:var(--text2); font-size:12px; font-weight:600; }

    .pempty { padding:40px 20px; text-align:center; color:var(--text2); font-size:13.5px; line-height:1.8; }
    .pempty a { color:var(--accent); font-weight:700; text-decoration:none; }

    /* Monthly study calendar */
    .calendar-wrap { padding:18px; }
    .calendar-shell { display:grid; grid-template-columns:minmax(0,1fr) 210px; gap:14px; align-items:stretch; }
    .calendar-main { min-width:0; padding:16px; border:1px solid var(--border); border-radius:16px; background:var(--surface); }
    .calendar-nav { display:flex; align-items:center; gap:8px; }
    .calendar-nav button { width:32px; height:32px; border:1px solid var(--border); border-radius:50%; background:var(--surface2); color:var(--text); cursor:pointer; font-size:20px; line-height:1; }
    .calendar-nav button:hover { background:var(--accent); color:#fff; }
    .calendar-nav span { min-width:108px; text-align:center; color:var(--text); font-size:13px; font-weight:800; text-transform:capitalize; }
    .month-calendar { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:0; }
    .month-calendar :global(.calendar-month-block) { min-width:0; padding:2px 18px 8px; }
    .month-calendar :global(.calendar-month-block + .calendar-month-block) { border-left:1px solid var(--border); }
    .month-calendar :global(.calendar-month-title) { margin:0 0 14px; text-align:center; color:var(--text); font-size:14px; font-weight:800; text-transform:capitalize; }
    .month-calendar :global(.calendar-weekdays), .month-calendar :global(.calendar-grid) { display:grid; grid-template-columns:repeat(7,minmax(0,1fr)); gap:5px 3px; }
    .month-calendar :global(.calendar-weekdays) { margin-bottom:5px; }
    .month-calendar :global(.calendar-weekdays span) { padding:5px 0; text-align:center; color:var(--text2); font-size:10px; font-weight:800; text-transform:uppercase; }
    .month-calendar :global(.cal-day) { width:36px; height:36px; justify-self:center; border:0; background:transparent; border-radius:50%; padding:0; color:var(--text); position:relative; display:grid; place-items:center; cursor:pointer; font-family:inherit; transition:background .16s,color .16s,transform .16s,box-shadow .16s; }
    .month-calendar :global(.cal-day:hover:not(.empty)) { transform:scale(1.08); background:var(--surface2); }
    .month-calendar :global(.cal-day.empty) { cursor:default; opacity:0; pointer-events:none; }
    .month-calendar :global(.cal-num) { display:grid; place-items:center; font-size:12px; font-weight:700; line-height:1; }
    .month-calendar :global(.cal-day.active) { background:color-mix(in srgb,var(--accent) 14%,var(--surface)); color:var(--accent); }
    .month-calendar :global(.cal-day.selected) { background:var(--text); color:var(--surface); box-shadow:0 7px 18px color-mix(in srgb,var(--text) 24%,transparent); }
    .month-calendar :global(.cal-day.today:not(.selected)) { box-shadow:inset 0 0 0 2px var(--accent); color:var(--accent); }
    .month-calendar :global(.cal-count) { position:absolute; right:-1px; top:-1px; min-width:14px; height:14px; padding:0 3px; display:grid; place-items:center; border-radius:999px; background:var(--accent); color:#fff; font-size:7px; font-weight:900; }
    .cal-note { margin-top:12px; color:var(--text2); font-size:11.5px; display:flex; align-items:center; gap:7px; }
    .cal-note::before { content:''; width:9px; height:9px; border-radius:3px; background:var(--accent); }
    .calendar-agenda { min-width:0; padding:16px; border:1px solid var(--border); border-radius:16px; background:linear-gradient(155deg,color-mix(in srgb,var(--accent) 8%,var(--surface)),var(--surface)); }
    .agenda-title { color:var(--text); font-size:13px; font-weight:850; margin:0 0 12px; padding-bottom:11px; border-bottom:1px solid var(--border); }
    .agenda-list { display:flex; flex-direction:column; gap:9px; }
    .agenda-list :global(.agenda-item) { border:1px solid var(--border); background:var(--surface); border-radius:11px; padding:11px; }
    .agenda-list :global(.agenda-item b) { display:block; color:var(--text); font-size:11.5px; line-height:1.35; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
    .agenda-list :global(.agenda-item span) { display:block; color:var(--accent); font-size:10px; font-weight:800; margin-top:4px; }
    .agenda-list :global(.agenda-empty) { color:var(--text2); font-size:12px; line-height:1.55; padding:6px 0; }

    /* Leaderboards */
    .honor { margin-top:28px; animation:fadeUp .5s .2s ease both; }
    .leader-grid { display:grid; grid-template-columns:repeat(2,1fr); gap:18px; }
    .leader-card { background:var(--surface); border:1px solid var(--border); border-top:3px solid var(--leader,var(--accent)); border-radius:var(--r); padding:18px; min-height:190px; }
    .leader-title { font-size:12.5px; font-weight:900; text-transform:uppercase; color:var(--text); display:flex; align-items:center; gap:7px; }
    .leader-list { margin-top:15px; display:flex; flex-direction:column; gap:8px; }
    .leader-row { display:grid; grid-template-columns:28px 34px minmax(0,1fr) auto; gap:9px; align-items:center; padding:7px 0; }
    .leader-rank { width:25px; height:25px; display:grid; place-items:center; border-radius:50%; background:var(--surface2); color:var(--text2); font-size:11px; font-weight:900; }
    .leader-row:first-child .leader-rank { background:#fbbf24; color:#5b3700; }
    .leader-avatar { width:32px; height:32px; display:grid; place-items:center; border-radius:50%; color:#fff; background:linear-gradient(135deg,#2563eb,#7c3aed); font-size:11px; font-weight:900; overflow:hidden; }
    .leader-avatar img { width:100%; height:100%; object-fit:cover; }
    .leader-name { min-width:0; color:var(--text); font-size:12.5px; font-weight:750; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
    .leader-value { color:var(--leader,var(--accent)); font-size:12px; font-weight:900; white-space:nowrap; }
    .leader-empty { color:var(--text2); font-size:12.5px; line-height:1.65; padding:20px 4px 4px; }

    /* System stats */
    .sys-stat b { font-size:22px; color:var(--text); }

    /* Toast */
    .toast { position:fixed; top:16px; left:50%; transform:translateX(-50%) translateY(-80px); z-index:999; padding:12px 24px; border-radius:12px; font-size:14px; font-weight:700; transition:transform .35s cubic-bezier(.34,1.56,.64,1); pointer-events:none; }
    .toast.s { background:rgba(34,197,94,.92); color:#fff; }
    .toast.e { background:rgba(239,68,68,.92); color:#fff; }
    .toast.show { transform:translateX(-50%) translateY(0); }

    /* Loading */
    .loader { position:fixed; inset:0; background:var(--bg); display:grid; place-items:center; z-index:999; transition:opacity .4s; }
    .loader.gone { opacity:0; pointer-events:none; }
    .spinner { width:44px; height:44px; border:3px solid var(--surface2); border-top-color:var(--accent); border-radius:50%; animation:spin .8s linear infinite; }
    @keyframes spin { to { transform:rotate(360deg); } }

    /* Sidebar overlay */
    .sb-overlay { display:none; position:fixed; inset:0; background:rgba(0,0,0,.6); z-index:90; opacity:0; pointer-events:none; transition:opacity .3s; }

    /* Responsive */
    @media(max-width:1100px) { .qgrid { grid-template-columns:repeat(2,1fr); } .dgrid { grid-template-columns:1fr; } .leader-grid { grid-template-columns:1fr 1fr; } }
    @media(max-width:900px) {
      .sidebar { transform:translateX(-100%); }
      .sidebar.open { transform:translateX(0); }
      .sb-overlay { display:block; }
      .sb-overlay.open { opacity:1; pointer-events:auto; }
      .main { margin-left:0; }
      .hbtn { display:grid; place-items:center; }
      .stats { grid-template-columns:repeat(2,1fr); }
    }
    @media(max-width:760px) { .calendar-shell { grid-template-columns:1fr; } .calendar-main { padding:12px; } .calendar-agenda { padding:14px; } .month-calendar { grid-template-columns:1fr; } .month-calendar :global(.calendar-month-block) { padding:2px 6px 8px; } .month-calendar :global(.calendar-month-block:nth-child(2)) { display:none; } }
    @media(max-width:650px) { .calendar-wrap { padding:10px; } .month-calendar :global(.calendar-grid) { gap:3px 1px; } .month-calendar :global(.cal-day) { width:32px; height:32px; } .month-calendar :global(.cal-num) { font-size:11px; } .month-calendar :global(.cal-count) { min-width:13px; height:13px; font-size:7px; } .leader-grid { grid-template-columns:1fr; } }
    @media(max-width:540px) { .content { padding:16px; } .hero { padding:24px; flex-direction:column; } .qgrid { grid-template-columns:1fr; } .topbar { padding:0 16px; } }
  </style>
</head>
<body>

<div class="loader" id="loader"><div class="spinner"></div></div>
<div class="toast" id="toast"></div>
<div class="sb-overlay" id="sb-overlay"></div>

<!-- SIDEBAR -->
<aside class="sidebar" id="sidebar">
  <a href="/dashboard.html" class="sb-logo">
    <div class="sb-logo-mark">2TF</div>
    <div><b>2TF529</b><small>Sổ tay luyện thi</small></div>
  </a>

  <div class="sb-user">
    <div class="sb-avatar" id="sb-av"><span id="sb-init">2T</span><img id="sb-av-img" alt="" hidden></div>
    <div>
      <div class="sb-uname" id="sb-name">Đang tải...</div>
      <div class="sb-usub" id="sb-sub">—</div>
    </div>
  </div>

  <nav class="sb-nav">
    <span class="sb-section">Menu chính</span>
    <a href="/dashboard.html" class="sb-link active"><span class="ni">🏠</span> Trang chủ</a>
    <a href="/explore.html" class="sb-link"><span class="ni">📚</span> Tìm đề thi</a>
    <a href="/on-tap.html" class="sb-link"><span class="ni">✏️</span> Ôn theo dạng</a>
    <a href="/lich-su.html" class="sb-link"><span class="ni">📋</span> Lịch sử làm bài</a>

    <span class="sb-section" style="margin-top:8px;">Cá nhân</span>
    <a href="/tai-khoan.html" class="sb-link"><span class="ni">👤</span> Hồ sơ của tôi</a>
    <a href="/ung-ho.html" class="sb-link"><span class="ni">💙</span> Ủng hộ 2TF</a>
  </nav>

  <div class="sb-bottom">
    <button class="sb-logout" id="dash-logout"><span>🚪</span> Đăng xuất</button>
  </div>
</aside>

<!-- MAIN -->
<div class="main">
  <header class="topbar">
    <div class="topbar-l">
      <button class="hbtn" id="hbtn" aria-label="Menu">☰</button>
      <div>
        <div class="topbar-title">Dashboard</div>
        <div class="topbar-sub" id="topbar-date"></div>
      </div>
    </div>
    <div class="topbar-r">
      <button class="topbar-btn" id="theme-toggle-db" title="Đổi theme">🌙</button>
      <a href="/tai-khoan.html" class="topbar-profile">⚙ Hồ sơ</a>
    </div>
  </header>

  <div class="content">

    <!-- Hero -->
    <div class="hero" id="hero">
      <div class="hero-text">
        <div class="hero-greet" id="greet-time">Chào buổi sáng ☀️</div>
        <div class="hero-name" id="hero-name">Bạn ơi 👋</div>
        <div class="hero-goal">Mục tiêu: <span class="goal-chip" id="hero-goal">Chưa đặt nguyện vọng</span></div>
      </div>
      <div class="hero-streak">
        <div class="streak-fire">🔥</div>
        <div class="streak-num" id="streak-n">0</div>
        <div class="streak-lbl">Ngày học liên tiếp</div>
      </div>
    </div>

    <!-- Stats row -->
    <div class="stats">
      <div class="stat blue">
        <div class="stat-hd"><span class="stat-lbl">Đề đã làm</span><span class="stat-ico">📝</span></div>
        <div class="stat-val" id="ds-exams">0</div>
        <div class="stat-sub">Tổng số lượt hoàn thành</div>
      </div>
      <div class="stat green">
        <div class="stat-hd"><span class="stat-lbl">Ngày học</span><span class="stat-ico">📅</span></div>
        <div class="stat-val" id="ds-days">0</div>
        <div class="stat-sub">Số ngày có hoạt động</div>
      </div>
      <div class="stat amber">
        <div class="stat-hd"><span class="stat-lbl">Điểm TB</span><span class="stat-ico">⭐</span></div>
        <div class="stat-val" id="ds-score">—</div>
        <div class="stat-sub">Trên thang điểm 10</div>
      </div>
      <button class="stat random-study-stat" id="random-study" type="button">
        <div class="stat-hd"><span class="stat-lbl">Chọn giúp tôi</span><span class="stat-ico">✨</span></div>
        <div class="stat-val">Nếu bạn không biết học gì</div>
        <div class="stat-sub" id="random-study-count" aria-live="polite">Đang đếm danh sách…</div>
      </button>
    </div>

    <!-- Quick start -->
    <div class="stitle">🚀 Bắt đầu học ngay</div>
    <div class="qgrid">
      <a href="/explore.html?grade=l12&type=totnghiep" class="qcard" style="--qa:#4f8ef7">
        <div class="qcard-ico">🎓</div>
        <div class="qcard-title">Thi Tốt nghiệp THPT</div>
        <div class="qcard-desc">Đề thi giống giao diện Bộ Giáo dục, có đáp án chuẩn chính thức.</div>
        <div class="qcard-arrow">Luyện ngay →</div>
      </a>
      <a href="/explore.html?grade=l12" class="qcard" style="--qa:#22c55e">
        <div class="qcard-ico">📘</div>
        <div class="qcard-title">Đề Khối 12</div>
        <div class="qcard-desc">Toàn bộ đề thi khối 12 từ các tỉnh và trường trên cả nước.</div>
        <div class="qcard-arrow">Xem đề →</div>
      </a>
      <a href="/on-tap.html" class="qcard" style="--qa:#f59e0b">
        <div class="qcard-ico">✏️</div>
        <div class="qcard-title">Ôn theo dạng bài</div>
        <div class="qcard-desc">Chọn chủ đề, dạng bài để ôn tập có chọn lọc và hiệu quả hơn.</div>
        <div class="qcard-arrow">Ôn tập →</div>
      </a>
      <a href="/explore.html?subject=tong-hop" class="qcard" style="--qa:#a855f7">
        <div class="qcard-ico">🧠</div>
        <div class="qcard-title">Đánh giá Năng lực</div>
        <div class="qcard-desc">TSA · V-ACT · V-SAT · HSA · BCA · QĐA và các kỳ thi đặc biệt.</div>
        <div class="qcard-arrow">Xem thêm →</div>
      </a>
      <a href="/explore.html?type=hsg" class="qcard" style="--qa:#ef4444">
        <div class="qcard-ico">🏅</div>
        <div class="qcard-title">Học sinh Giỏi</div>
        <div class="qcard-desc">Đề thi HSG các cấp — tỉnh, quốc gia. Luyện thêm để vượt trội.</div>
        <div class="qcard-arrow">Chinh phục →</div>
      </a>
      <a href="/explore.html?group=ngoai-ngu" class="qcard" style="--qa:#0ea5e9">
        <div class="qcard-ico">🌐</div>
        <div class="qcard-title">Ngoại ngữ</div>
        <div class="qcard-desc">IELTS · TOEIC · HSK · TOPIK · JLPT · VSTEP · APTIS đầy đủ.</div>
        <div class="qcard-arrow">Học ngoại ngữ →</div>
      </a>
    </div>

    <!-- Dashboard grid -->
    <div class="dgrid">

      <!-- Lịch học -->
      <div class="panel">
        <div class="panel-hd">
          <div class="panel-hd-t">📆 Lịch học tháng này</div>
          <div class="calendar-nav"><button type="button" id="calendar-prev" aria-label="Tháng trước">‹</button><span id="calendar-month"></span><button type="button" id="calendar-next" aria-label="Tháng sau">›</button></div>
        </div>
        <div class="calendar-wrap">
          <div class="calendar-shell">
            <div class="calendar-main">
              <div class="month-calendar" id="month-calendar"></div>
              <div class="cal-note">Ngày có hoàn thành bài luyện tập</div>
            </div>
            <aside class="calendar-agenda">
              <div class="agenda-title" id="agenda-title">Hoạt động hôm nay</div>
              <div class="agenda-list" id="agenda-list"></div>
            </aside>
          </div>
        </div>
      </div>

      <!-- Right column -->
      <div style="display:flex;flex-direction:column;gap:20px;">

        <!-- Recent history -->
        <div class="panel">
          <div class="panel-hd">
            <div class="panel-hd-t">📋 Làm bài gần đây</div>
            <a href="/lich-su.html" class="panel-hd-a">Xem tất cả →</a>
          </div>
          <div class="panel-body" id="dash-hist">
            <div class="pempty">Đang tải...</div>
          </div>
        </div>

        <!-- System stats -->
        <div class="panel">
          <div class="panel-hd"><div class="panel-hd-t">🌐 Hệ thống 2TF529</div></div>
          <div class="panel-body" style="padding:16px 20px;">
            <div id="sys-stats" class="sys-stat" style="color:var(--text2);font-size:13.5px;line-height:2.2;">Đang tải...</div>
          </div>
        </div>

        <!-- Community -->
        <div class="panel">
          <div class="panel-hd"><div class="panel-hd-t">📣 Cộng đồng</div></div>
          <div class="panel-body" style="padding:10px;">
            <a href="https://t.me/ttfshool" target="_blank" rel="noopener" class="qcard" style="--qa:#0ea5e9;padding:14px;margin-bottom:8px;">
              <div style="display:flex;align-items:center;gap:10px;">
                <span style="font-size:22px;">✈️</span>
                <div><div class="qcard-title" style="font-size:13px;">Nhóm Telegram</div><div class="qcard-desc" style="font-size:11.5px;">t.me/ttfshool</div></div>
              </div>
            </a>
            <a href="https://discord.gg/WS4VdsYxtQ" target="_blank" rel="noopener" class="qcard" style="--qa:#5865f2;padding:14px;">
              <div style="display:flex;align-items:center;gap:10px;">
                <span style="font-size:22px;">🎮</span>
                <div><div class="qcard-title" style="font-size:13px;">Nhóm Discord</div><div class="qcard-desc" style="font-size:11.5px;">discord.gg/WS4VdsYxtQ</div></div>
              </div>
            </a>
          </div>
        </div>

      </div>
    </div>

    <section class="honor" id="bang-vinh-danh">
      <div class="stitle">🏆 Bảng Vinh Danh</div>
      <div class="leader-grid">
        <article class="leader-card" style="--leader:#2563eb">
          <div class="leader-title">⛏️ Top Cày Cuốc</div>
          <div class="leader-list" id="leader-diligent"></div>
        </article>
        <article class="leader-card" style="--leader:#ef4444">
          <div class="leader-title">🔥 Top Giữ Lửa</div>
          <div class="leader-list" id="leader-streak"></div>
        </article>
      </div>
    </section>
  </div>
</div>

<script is:inline src={`/js/security.js?v=${v}`}></script>
<script is:inline src={`/js/shield.js?v=${v}`}></script>
<script is:inline src={`/js/supabase.js?v=${v}`} defer></script>
<script is:inline src={`/js/sw-register.js?v=${v}`} defer></script>
<script is:inline src={`/js/calculator.js?v=${v}`} defer></script>
<script is:inline src={`/js/whiteboard.js?v=${v}`} defer></script>
<script is:inline src={`/js/periodic-table.js?v=${v}`} defer></script>
<script is:inline>
(function () {
  const $ = id => document.getElementById(id);
  let _tt;
  let calendarOffset = 0;
  let calendarHistory = [];
  let selectedCalendarKey = '';
  let randomStudyLinks = [];

  function toast(txt, type='s') {
    const el = $('toast');
    el.textContent = txt;
    el.className = `toast ${type} show`;
    clearTimeout(_tt);
    _tt = setTimeout(() => el.classList.remove('show'), 3200);
  }

  function initials(name) {
    return (name||'2TF').split(/\s+/).filter(Boolean).slice(-2).map(x=>x[0]).join('').toUpperCase();
  }

  function greeting() {
    const h = new Date().getHours();
    if (h < 6)  return 'Thức khuya học bài rồi 🌙';
    if (h < 12) return 'Chào buổi sáng ☀️';
    if (h < 18) return 'Chào buổi chiều 🌤';
    return 'Chào buổi tối 🌆';
  }

  function fmtDate(iso) {
    return new Date(iso).toLocaleDateString('vi-VN', {day:'2-digit',month:'2-digit',year:'numeric'});
  }

  function localDateKey(value) {
    const d = value instanceof Date ? value : new Date(value);
    if (Number.isNaN(d.getTime())) return '';
    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
  }

  function scoreClass(s) {
    if (typeof s !== 'number') return 'd';
    return s >= 8 ? 'g' : s >= 5 ? 'o' : 'b';
  }

  function computeStats() {
    let hist = [];
    try { hist = JSON.parse(localStorage.getItem('examHistory')||'[]'); } catch {}
    const days = [...new Set(hist.map(x=>localDateKey(x.date)).filter(Boolean))].sort().reverse();
    let streak = 0;
    const cur = new Date(); cur.setHours(0,0,0,0);
    if (days[0]) {
      const latest = new Date(days[0]+'T00:00:00');
      if ((cur-latest)/86400000 <= 1) {
        if (latest < cur) cur.setDate(cur.getDate()-1);
        const set = new Set(days);
        while (set.has(localDateKey(cur))) { streak++; cur.setDate(cur.getDate()-1); }
      }
    }
    const scored = hist.filter(x=>typeof x.score==='number');
    return {
      hist,
      days: days.length,
      streak,
      avg: scored.length ? (scored.reduce((s,x)=>s+x.score,0)/scored.length).toFixed(1) : '—'
    };
  }

  function renderAgenda(hist,key) {
    const items = hist.filter(item=>localDateKey(item.date)===key);
    const date = key ? new Date(`${key}T12:00:00`) : new Date();
    $('agenda-title').textContent = `Hoạt động ${date.toLocaleDateString('vi-VN',{day:'2-digit',month:'2-digit'})}`;
    if (!items.length) {
      $('agenda-list').innerHTML = '<div class="agenda-empty">Chưa có hoạt động học tập trong ngày này.</div>';
      return;
    }
    const esc = value => { const d=document.createElement('div'); d.textContent=value||''; return d.innerHTML; };
    $('agenda-list').innerHTML = items.slice(0,6).map(item=>`<div class="agenda-item"><b>${esc(item.title||'Đề thi')}</b><span>${typeof item.score==='number'?`${item.score.toFixed(1)} điểm`:'Đã hoàn thành'}</span></div>`).join('');
  }

  function renderMonthCalendar(hist) {
    const now = new Date();
    const view = new Date(now.getFullYear(),now.getMonth()+calendarOffset,1);
    const nextView = new Date(view.getFullYear(),view.getMonth()+1,1);
    const counts = hist.reduce((map,item) => {
      const key = localDateKey(item.date);
      if (key) map.set(key,(map.get(key)||0)+1);
      return map;
    }, new Map());
    const firstLabel = view.toLocaleDateString('vi-VN',{month:'long'});
    const secondLabel = nextView.toLocaleDateString('vi-VN',{month:'long'});
    $('calendar-month').textContent = view.getFullYear()===nextView.getFullYear()
      ? `${firstLabel} – ${secondLabel}, ${view.getFullYear()}`
      : `${firstLabel} ${view.getFullYear()} – ${secondLabel} ${nextView.getFullYear()}`;
    const visiblePrefixes = [view,nextView].map(date=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}`);
    if (!visiblePrefixes.some(prefix=>selectedCalendarKey.startsWith(prefix))) {
      selectedCalendarKey = calendarOffset===0 ? localDateKey(now) : `${visiblePrefixes[0]}-01`;
    }
    const root = $('month-calendar');
    root.textContent = '';
    [view,nextView].forEach(monthDate=>{
      const year = monthDate.getFullYear();
      const month = monthDate.getMonth();
      const block = document.createElement('section');
      block.className = 'calendar-month-block';
      const title = document.createElement('h3');
      title.className = 'calendar-month-title';
      title.textContent = monthDate.toLocaleDateString('vi-VN',{month:'long',year:'numeric'});
      const weekdays = document.createElement('div');
      weekdays.className = 'calendar-weekdays';
      weekdays.innerHTML = '<span>T2</span><span>T3</span><span>T4</span><span>T5</span><span>T6</span><span>T7</span><span>CN</span>';
      const grid = document.createElement('div');
      grid.className = 'calendar-grid';
      const offset = (new Date(year,month,1).getDay()+6)%7;
      const total = new Date(year,month+1,0).getDate();
      for (let i=0;i<offset;i++) {
        const empty = document.createElement('span');
        empty.className = 'cal-day empty';
        grid.appendChild(empty);
      }
      for (let day=1;day<=total;day++) {
        const key = localDateKey(new Date(year,month,day));
        const count = counts.get(key)||0;
        const el = document.createElement('button');
        el.type = 'button';
        el.className = `cal-day${count?' active':''}${key===localDateKey(now)?' today':''}${key===selectedCalendarKey?' selected':''}`;
        el.title = count ? `${day}/${month+1}: ${count} bài đã làm` : `${day}/${month+1}: chưa có hoạt động`;
        el.setAttribute('aria-label',el.title);
        el.innerHTML = `<span class="cal-num">${day}</span>${count?`<span class="cal-count">${count}</span>`:''}`;
        el.addEventListener('click',()=>{ selectedCalendarKey=key; renderMonthCalendar(hist); });
        grid.appendChild(el);
      }
      block.append(title,weekdays,grid);
      root.appendChild(block);
    });
    renderAgenda(hist,selectedCalendarKey);
  }

  function renderHistory(hist) {
    const el = $('dash-hist');
    if (!hist.length) {
      el.innerHTML = `<div class="pempty">Bạn chưa làm đề nào.<br><a href="/explore.html">Tìm đề để bắt đầu →</a></div>`;
      return;
    }
    const esc = v => { const d=document.createElement('div'); d.textContent=v||''; return d.innerHTML; };
    el.innerHTML = hist.slice(0,4).map(h => {
      const sc = typeof h.score==='number' ? h.score.toFixed(1) : 'Đã làm';
      const cls = scoreClass(h.score);
      return `<a href="/thi.html?id=${encodeURIComponent(h.examId||'')}" class="hrow">
        <div style="min-width:0"><div class="hrow-t">${esc(h.title||'Đề thi')}</div><div class="hrow-d">${fmtDate(h.date)}</div></div>
        <div class="hscore ${cls}">${sc}</div>
      </a>`;
    }).join('');
  }

  function renderLeaderboards(name, avatarUrl, hist, streak) {
    const esc = value => { const d=document.createElement('div'); d.textContent=value||''; return d.innerHTML; };
    const escAttr = value => String(value||'').replace(/[&"<>]/g,ch=>({'&':'&amp;','"':'&quot;','<':'&lt;','>':'&gt;'}[ch]));
    const avatar = avatarUrl ? `<img src="${escAttr(avatarUrl)}" alt="">` : esc(initials(name));
    const row = value => `<div class="leader-row"><span class="leader-rank">1</span><span class="leader-avatar">${avatar}</span><span class="leader-name">${esc(name)}</span><span class="leader-value">${value}</span></div>`;
    $('leader-diligent').innerHTML = hist.length ? row(`${hist.length} đề`) : '<div class="leader-empty">Hoàn thành đề đầu tiên để bắt đầu hành trình cày cuốc.</div>';
    $('leader-streak').innerHTML = streak ? row(`${streak} ngày 🔥`) : '<div class="leader-empty">Học liên tiếp mỗi ngày để bắt đầu chuỗi giữ lửa.</div>';
  }

  function parseRandomStudyLinks(text) {
    const source = text.split(/\r?\n/).slice(4)
      .map(line=>line.trim())
      .filter(line=>line && !line.startsWith('#'))
      .map(line=>line.match(/https?:\/\/\S+/i)?.[0] || '')
      .map(url=>url.replace(/[),.;]+$/,''));
    return [...new Set(source.filter(Boolean).filter(url=>{
      try { return ['http:','https:'].includes(new URL(url,location.origin).protocol); }
      catch { return false; }
    }))];
  }

  async function loadRandomStudyLinks(force=false) {
    if (randomStudyLinks.length && !force) return randomStudyLinks;
    const response = await fetch('/data/random-study-links.txt',{cache:'no-store'});
    if (!response.ok) throw new Error('Không tải được danh sách');
    randomStudyLinks = parseRandomStudyLinks(await response.text());
    $('random-study-count').textContent = `Random 1 trong ${randomStudyLinks.length.toLocaleString('vi-VN')}`;
    return randomStudyLinks;
  }

  async function openRandomStudy() {
    const button = $('random-study');
    button.disabled = true;
    const tab = window.open('about:blank','_blank');
    if (!tab) {
      toast('Trình duyệt đang chặn tab mới. Hãy cho phép cửa sổ bật lên.','e');
      button.disabled = false;
      return;
    }
    tab.opener = null;
    try {
      const links = await loadRandomStudyLinks(true);
      if (!links.length) { tab.close(); toast('Bạn chưa thêm link vào data/random-study-links.txt','e'); return; }
      const chosen = links[Math.floor(Math.random()*links.length)];
      tab.location.replace(chosen);
    } catch { tab.close(); toast('Danh sách gợi ý học chưa sẵn sàng.','e'); }
    finally { button.disabled = false; }
  }

  async function loadSysStats() {
    try {
      const r = await fetch('/data/stats.json', {cache:'no-store'});
      const s = await r.json();
      $('sys-stats').innerHTML =
        `<b>${s.examCount}</b> đề thi<br><b>${s.questionCount}</b> câu hỏi<br><b>${s.subjectCount}</b> môn học`;
    } catch {
      $('sys-stats').textContent = 'Không tải được số liệu.';
    }
  }

  function init() {
    const user = window.supabase?.getUser?.();
    if (!user) { window.location.replace('/tai-khoan.html'); return; }

    // Ẩn loader
    const loader = $('loader');
    loader.classList.add('gone');
    setTimeout(() => loader.style.display = 'none', 500);

    // User info
    const meta = user.user_metadata || {};
    const name = meta.username || meta.full_name || meta.name || user.email?.split('@')[0] || 'Bạn';
    const year = meta.graduation_year ? `Thi ${meta.graduation_year}` : 'Chưa chọn năm thi';
    const goal = meta.first_choice || 'Chưa đặt nguyện vọng';
    const avatarUrl = localStorage.getItem('account_avatar') || meta.avatar_url || '';

    $('sb-init').textContent = initials(name);
    $('sb-name').textContent = name;
    $('sb-sub').textContent = year;
    if (avatarUrl) { $('sb-av-img').src = avatarUrl; $('sb-av-img').hidden = false; }

    $('greet-time').textContent = greeting();
    $('hero-name').textContent = name + ' 👋';
    $('hero-goal').textContent = goal;

    const cover = localStorage.getItem('account_cover');
    if (cover) {
      const hero = $('hero');
      hero.style.backgroundImage = `linear-gradient(135deg,rgba(9,18,32,.88) 0%,rgba(9,18,32,.5) 100%),url(${cover})`;
      hero.style.backgroundSize = 'cover';
      hero.style.backgroundPosition = 'center';
    }

    $('topbar-date').textContent = new Date().toLocaleDateString('vi-VN', {weekday:'long',day:'2-digit',month:'long',year:'numeric'});

    const { hist, days, streak, avg } = computeStats();
    $('ds-exams').textContent = hist.length;
    $('ds-days').textContent = days;
    $('ds-score').textContent = avg;
    $('streak-n').textContent = streak;

    renderHistory(hist);
    calendarHistory = hist;
    selectedCalendarKey = localDateKey(new Date());
    renderMonthCalendar(hist);
    renderLeaderboards(name, avatarUrl, hist, streak);
    loadSysStats();
    loadRandomStudyLinks().catch(()=>$('random-study-count').textContent='Random chưa sẵn sàng');
    $('random-study').addEventListener('click',openRandomStudy);

    $('calendar-prev').addEventListener('click',()=>{ calendarOffset--; selectedCalendarKey=''; renderMonthCalendar(calendarHistory); });
    $('calendar-next').addEventListener('click',()=>{ calendarOffset++; selectedCalendarKey=''; renderMonthCalendar(calendarHistory); });

    // Logout
    $('dash-logout').addEventListener('click', async () => {
      await window.supabase.logout();
      window.location.replace('/tai-khoan.html');
    });

    // Mobile sidebar
    const sidebar = $('sidebar');
    const overlay = $('sb-overlay');
    $('hbtn').addEventListener('click', () => { sidebar.classList.toggle('open'); overlay.classList.toggle('open'); });
    overlay.addEventListener('click', () => { sidebar.classList.remove('open'); overlay.classList.remove('open'); });

    // Theme toggle
    const themeBtn = $('theme-toggle-db');
    const updateIcon = () => {
      themeBtn.textContent = document.documentElement.getAttribute('data-theme') === 'dark' ? '☀️' : '🌙';
    };
    updateIcon();
    themeBtn.addEventListener('click', () => {
      const next = document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
      document.documentElement.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);
      updateIcon();
    });
  }

  document.addEventListener('DOMContentLoaded', () => setTimeout(init, 120));
})();
</script>
</body>
</html>
