{
  "name": "2tf529-study-web",
  "version": "1.0.0",
  "description": "Web thi thử trực tuyến 2TF529 — 9.600+ đề thi, JAMstack trên Cloudflare Pages",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/copy-data-to-dist.mjs",
    "deploy": "wrangler deploy",
    "preview": "astro preview",
    "astro": "astro",
    "upload-r2": "node scripts/upload-webp-to-r2.mjs",
    "rewrite-urls": "node scripts/rewrite-image-urls.mjs",
    "setup-assets": "node scripts/setup-github-assets.mjs"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1116.0",
    "wrangler": "^4.125.0"
  }
}
